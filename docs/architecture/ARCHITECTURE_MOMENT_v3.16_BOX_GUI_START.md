# ARCHITECTURE_MOMENT_v3.16_BOX_GUI_START

**Дата:** 2026‑01‑13  
**Архитектор проекта:** Владимир  
**AI‑партнёр:** Copilot  

---

# 🟩 1. Суть архитектурного момента v3.16

Узел **v3.16_BOX_GUI_START** открывает новый этап развития StoicizmFrame:  
переход от чисто архитектурного ядра (Foundry, Router, Pipeline, QC, Health)  
к **премиум‑коробочной версии** с интерфейсом, кнопками и управляемым процессом генерации контента.

Цель момента — задать **каркас коробки** (BOX), через который архитектор/режиссёр управляет фабрикой контента.

---

# 🟦 2. Назначение коробочной версии

Коробочная версия StoicizmFrame в рамках v3.16 предназначена для того, чтобы:

- предоставить **единый пульт управления** контент‑фабрикой;
- дать возможность **запуска, остановки, перезапуска** пайплайна;
- обеспечить **настройку параметров генерации** без захода в код;
- визуализировать состояние **Pipeline / QC / Health**;
- показывать результаты генерации сцен и роликов;
- подготовить базу для дальнейшей автоматизации и монетизации.

Это **продуктный слой**, поверх уже существующего архитектурного ядра v3.15.

---

# 🟧 3. Основные зоны интерфейса BOX v3.16

В рамках v3.16 выделяются следующие базовые зоны коробки:

## 3.1. Зона выбора направления фабрики

- выбор одного из **4 направлений фабрики**;
- выбор типа сцены / формата ролика (например, SCENE_001, SCENE_002 и т.д.);
- подготовка к будущему расширению SCENE‑пула.

## 3.2. Зона параметров генерации

- выбор модели / режима (reasoning / creative / structured);
- базовые параметры глубины и длины;
- возможность указать источник/донора (в перспективе).

## 3.3. Зона запуска и управления процессом

- кнопки: **Запуск / Перезапуск / Остановить**;
- режимы: одиночная генерация / пакетная генерация;
- фиксация факта старта и завершения каждого запуска.

## 3.4. Зона состояния пайплайна, QC и Health

- индикация прогресса выполнения;
- отображение статуса: **успех / отклонено QC / ошибка Health**;
- краткие сообщения о причинах отказа (GateDecision).

## 3.5. Зона результатов

- список последних сцен/роликов;
- краткое текстовое превью;
- отметки о прохождении QC/Health;
- ссылки на файлы/ресурсы (видео, скрипты и т.д.).

---

# 🟨 4. Взаимодействие BOX с ядром системы

Коробка v3.16 не заменяет архитектурное ядро, а **оборачивает его**:

- **Router**: выбор модели и режима работы;
- **Foundry‑клиент**: вызов моделей через BOX;
- **ContentPipeline**: запуск пайплайна из интерфейса;
- **QC‑цепочка**: отображение результатов проверок;
- **Health‑слой**: индикация состояния системы;
- **публикационный слой** (YouTube/Telegram): подготовка к интеграции.

BOX выступает как **человеко‑ориентированный фасад** над существующей архитектурой.

---

# 🟫 5. Роль автоматизации и участия человека в v3.16

В рамках v3.16:

- **человек**:
  - выбирает направление и параметры;
  - запускает генерацию;
  - принимает решения (публиковать / переработать);
  - контролирует качество результата;

- **система**:
  - выполняет генерацию сцены/контента;
  - прогоняет QC и Health;
  - возвращает структурированный результат;
  - фиксирует статусы выполнения.

Такой баланс обеспечивает **премиум‑уровень контроля**:  
машина делает тяжёлую работу, человек сохраняет роль архитектора‑режиссёра.

---

# 🟪 6. Границы узла v3.16

В узел **v3.16_BOX_GUI_START** входят:

- определение концепции коробки;
- фиксация основных зон интерфейса;
- базовое описание потоков действий пользователя;
- подготовка к интеграции с Router / Pipeline / QC / Health.

В узел **НЕ входят** (переносится на последующие версии):

- проработка финального визуального дизайна;
- расширенная работа с донорами;
- глубокая премиум‑кинематографика (Фаза 2);
- полная автоматизация фабрики (Фаза 3).

---

# 🟩 7. Заключение архитектурного момента

Узел **v3.16_BOX_GUI_START** официально открывает этап:

- перехода от архитектурного ядра к продуктной коробке;
- формализации роли человека как архитектора‑режиссёра;
- подготовки StoicizmFrame к выходу на рынок как **премиум‑продукт**.

Этот документ является **архитектурной плитой момента** для v3.16  
и служит точкой отсчёта для дальнейшей реализации BOX‑слоя.
